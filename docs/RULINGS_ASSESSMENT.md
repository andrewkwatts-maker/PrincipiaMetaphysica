# Rulings Assessment — Seven Blocked Decisions, Options Scored

**Prepared:** 2026-08-25 · **Status:** evidence + recommendation only — the
rulings are the author's. Each option is scored on the requested axes:
**elegance** (does the number fall out of the structure?), **geometric**
(does it live in the b₃ = 24 / 163 / 288 architecture?), **accuracy**
(σ against the declared anchor), **physical soundness** (is the mechanism
a real one?), and **falsifiability** (bonus — can it die, and is the kill
condition stated in advance?).

Every number below is read from the live registry
(`AutoGenerated/parameters.json`) or the outstanding-issues register —
none is invented here. Where a recommendation retires a branch, the
branch stays on the books labelled FALSIFIED, per standing policy.

---

## R1 — θ₁₃: retire 9.594° = asin(1/6) as the headline?

| Option | Value | Status | vs NuFIT 8.58° ± 0.11° |
|---|---|---|---|
| A. `neutrino.theta13_derived` = asin(1/6) | 9.594° | DERIVED, zero-parameter | **≈ 9σ FAIL** |
| B. `pmns.theta_13_triality` | 8.647° | **CALIBRATED** (fitted_pmns = 2) | 0.61σ |
| C. `particle.theta_13_deg` | 8.669° | DERIVED | 0.81σ |
| D. `geometry.theta_13` | 8.54° | GEOMETRIC | 0.36σ |

**Assessment.** Option A is the most elegant number on the table —
sin θ₁₃ = 1/6 exactly, no knobs — and that is precisely why its ≈9σ miss
is meaningful. A zero-parameter prediction that misses by 9σ is not a
tension; it is a falsification that the framework's own posture requires
it to own. Elegance never outranks a 9σ miss.

Option B is the most accurate but is CALIBRATED with two fitted PMNS
parameters — it is a fit wearing a prediction's clothing and must not be
promoted to headline without that label travelling with it.

Options C and D are both derived, sub-1σ, and unfitted. C (8.669°) sits
on the particle-sector chain; D (8.54°) on the geometric chain. They
disagree with each other by 0.129° (≈1.2σ in anchor units) — that
disagreement is itself a registered shadow-derivation conflict and should
stay visible, not be resolved by deleting one.

**Recommendation:** label A **FALSIFIED** (keep the row — it is the
framework's best demonstration that its gates can kill something), demote
it from every headline, and promote **C** as the working headline with D
as the geometric cross-check. B remains on the books as CALIBRATED.
**Falsifiability bonus goes to A posthumously** — it was maximally
falsifiable and was in fact falsified; the system worked.

---

## R2 — S₈: growth-ODE branch over the analytic approximation?

| Option | Value | Provenance | Nearest anchors |
|---|---|---|---|
| A. Analytic friction formula | 0.7841 (`cosmology.s8_pm_predicted`) | closed-form approximation | KiDS 0.91σ · DES 0.48σ · Planck 3.68σ |
| B. Growth-ODE integration | 0.7909 | numerical integration of the same friction term | ≈3.0σ vs Planck; ~1σ weak-lensing |

**Assessment.** These are not two theories — B is the honest evaluation
of the equation that A approximates. When an approximation and its own
ODE disagree, the approximation loses; there is no physics question here,
only an arithmetic-hygiene one. Keeping A as the quoted value while B
exists would be exactly the anchor-shopping pattern the DE-plane gate was
built to end.

**Recommendation:** adopt **B** (the ODE value) as canonical; keep A as
the documented approximation with its error (0.0068) stated. The
suppression *mechanism* (friction kernel, β_eff = 0.065) is unchanged —
this ruling only fixes which evaluation of it is quoted. Falsifiability:
the prediction lands between the weak-lensing and Planck values, so
upcoming KiDS-Legacy/DES-Y6 tightening bites directly.

---

## R3 — n_s: canonical branch

| Option | Value | vs Planck 0.9649 ± 0.0042 |
|---|---|---|
| A. `cosmology.n_s` = `n_s_pred` (geometric) | 0.96364 | **0.30σ** |
| B. `cosmology.n_s_slow_roll` | 0.99960 | **8.3σ FAIL** |

**Assessment.** A is already the canonical entry and is the geometric
chain's own number — elegant, on-architecture, 0.3σ. B is the slow-roll
evaluation of the claimed inflaton potential, and its 8.3σ divergence is
a real finding: **either the potential identification is wrong, or the
slow-roll treatment of it is** — the geometric n_s cannot be credited to
that potential while its own slow-roll form contradicts it.

**Recommendation:** keep **A** canonical; keep **B** registered and loud
as an open potential-falsifier (register 1.x material, not a footnote).
Do not average, do not suppress. This is the same "the gate caught
something — leave it caught" posture as the C4 monotonicity episode.

---

## R4 — H₀: remove the 73.04 anchor from the shadow-derivation group?

**Facts.** The late-time H₀ group currently mixes framework outputs
(`H0_local` = 71.55, `H0_late_evolved` = 73.04, `H0_resolved` = 73.04)
with `H0_baseline_km_s_Mpc` = 73.04 — which is the SH0ES anchor restated
under a cosmology.* name. Comparing a derivation against a re-keyed copy
of the anchor manufactures agreement (the 73.04-vs-73.04 rows) and
manufactures conflict (71.55-vs-73.04 reads as an internal contradiction
when it is actually the framework's honest 1.43σ MARGINAL against data).

**Assessment.** The disposition ledger already encodes the principle:
*an anchor is data, not a competing prediction* (`desi.S8`, `planck.S8`,
`bounds.sum_m_nu_upper` are all DISTINCT on exactly this ground). Leaving
one anchor inside one group is an inconsistency in the ledger's own rule.

**Recommendation:** rule **yes** — disposition `H0_baseline_km_s_Mpc`
as DISTINCT (anchor restatement), leaving the group to compare genuine
rivals: 71.55 vs 73.04-evolved (a real 2%-level internal spread that
deserves its conflict row) with 76.34 already DISTINCT under the prior
fitted-31° ruling. No score table needed; this is the ledger applying
its own stated principle uniformly.

---

## R5 — Canonical neutrino mass sum (decides the DESI-bound side)

| Option | Value (eV) | Status | DESI ΛCDM < 0.072 | w₀wₐCDM < 0.16 |
|---|---|---|---|---|
| A. `spectral.sum_m_nu` | 0.0598 | PREDICTED (currently UNBOUNDED, reg. 2.4) | **PASS** | PASS |
| B. `geometry.sum_m_nu` | 0.0817 | GEOMETRIC | FAIL | PASS |
| C. `neutrino.mass_sum` | 0.1012 | DERIVED | FAIL | PASS |

**Assessment.** A is the only branch that passes the ΛCDM bound
unconditionally, and it sits essentially at the normal-hierarchy floor
implied by the oscillation splittings (Σ ≥ ~0.059 eV from NuFIT Δm²) —
the most physically distinguished point on the table. Its weakness is
bookkeeping, not physics: it is one of the 17 UNBOUNDED entries, so its
derivation chain has had the least scrutiny. B and C survive only
conditionally — through the framework's own w₀wₐ cosmology via the
DE-plane gate's mass-sum conditional.

**Accuracy note:** A is *maximally* falsifiable downward — any future
detection of Σm_ν meaningfully above ~0.06 eV (or inverted hierarchy)
kills it outright, whereas B and C have room. Conversely, if cosmology
tightens below 0.072 under ΛCDM, A is the only survivor.

**Recommendation:** adopt **A** as canonical **contingent on a chain
audit** — bind it to the DESI anchor first (closing part of register
2.4), verify the spectral chain contains no calibrated step, and only
then move the headline. If the audit finds a fitted input, fall back to
B (geometric, on-architecture) and let the DE-plane conditional carry it.
Keep all three registered; the mass-sum conditional in the DE-plane gate
already reports every row against both bounds, so no information is lost
whichever is canonical.

---

## R6 — Theory tolerances for G12 / G30 / G32

**G12** (needs a theory-uncertainty budget; computes to ≈22.8σ without
one). Converting it with an invented tolerance sized to admit the miss
would be the textbook fake-pass — the same disease as the rejected 5%
Higgs buffer. **Recommendation: convert to COMPUTED and let it read
COMPUTED_FAIL** until a *cited* theory uncertainty exists (published
higher-order estimate, not a number chosen to fit). An honest FAIL is
strictly more informative than a DECLARATIVE shrug; this is the DE-plane
posture applied to a gate.

**G30** (claim contains "~", no numeric content). A gate that cannot
state its own tolerance cannot be computed without inventing one.
**Recommendation: stays DECLARATIVE**, with the blocker recorded — the
fix is upstream (sharpen the claim), not downstream (soften the check).

**G32** (sin²θ_W = 3/8 at unification). This is an exact rational at the
GUT boundary — the group-theory value for SU(5)/SO(10)-type embeddings —
so it is a Strategy-A-shaped semantic assertion, not a tolerance
question. **Recommendation: convert exactly** (assert the 3/8 against
the registry's unification value, no tolerance), making it the eighth
semantic gate. Note the register's separate warning: the *advisory*
formula (3/8)(1 − 2/24π) = 0.365 vs 0.2312 at M_Z was already falsified
this cycle — G32's claim must stay pinned at the GUT scale, where 3/8 is
sound, and never be silently rebased to M_Z.

---

## R7 — G36 threshold source

**Options for the CKM first-row check:**

| Option | Threshold | Character |
|---|---|---|
| A. Keep "< 1.0" | unitarity sum below 1 | vacuous half-window, cannot catch a low miss |
| B. PDG first-row unitarity | 0.9985 ± 0.0007 (PDG 2024) | cited measurement, two-sided |

**Assessment.** A is the strategy report's own flagged weakness — the
only Strategy-B threshold that wasn't sourced. B replaces it with the
actual measured first-row sum, which is currently *itself* ≈2σ below
unity (the Cabibbo-angle anomaly) — meaning a two-sided check against B
is a live, moving test, not a formality. With `ckm.V_us` = 0.2231 vs PDG
0.225, the framework's row sum will be probed at real precision.

**Recommendation:** adopt **B**. It is the only option on the table that
is simultaneously sourced, two-sided, and capable of failing in both
directions. If the gate then fails, it fails against the PDG — which is
what a gate is for.

---

## Summary of recommendations

| # | Ruling | Recommendation | Falsifiability |
|---|---|---|---|
| R1 | θ₁₃ headline | asin(1/6) → FALSIFIED (kept, labelled); 8.669° headline, 8.54° cross-check | asin(1/6) died honestly |
| R2 | S₈ branch | growth-ODE 0.7909 canonical; analytic kept as approximation | KiDS-Legacy / DES-Y6 |
| R3 | n_s branch | geometric 0.9636 canonical; slow-roll 8.3σ stays loud as potential-falsifier | CMB-S4 tightens |
| R4 | H₀ anchor-in-group | remove (DISTINCT: anchor restatement) — ledger's own rule | honest 1.43σ MARGINAL stands |
| R5 | mass sum | spectral 0.0598 canonical **after chain audit**; else geometric 0.0817 + conditional | Σm_ν detection or ΛCDM bound < 0.072 |
| R6 | G12/G30/G32 | G12 → COMPUTED_FAIL (honest); G30 stays DECLARATIVE; G32 → semantic 3/8 exact | G12 fails in public |
| R7 | G36 threshold | PDG first-row 0.9985 ± 0.0007, two-sided | Cabibbo-anomaly-live |

**Cross-cutting principle applied throughout:** where an option required
inventing a number (a tolerance, a correlation, an uncertainty) to reach
PASS, it lost — an honest FAIL or a labelled FALSIFIED outranks a
manufactured agreement on every axis that matters here.
