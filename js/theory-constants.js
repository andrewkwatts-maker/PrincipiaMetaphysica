/**
 * theory-constants.js - Principia Metaphysica Theory Constants
 *
 * AUTO-GENERATED from config.py by generate_js_constants.py
 * DO NOT EDIT THIS FILE MANUALLY - Edit config.py instead!
 *
 * Generated: 2025-11-27 19:22:34
 * Framework Version: v6.1
 *
 * To regenerate after editing config.py:
 *   python generate_js_constants.py
 */

const TheoryConstants = {

    // ================================================================
    // DIMENSIONAL STRUCTURE
    // ================================================================

    dimensions: {
        full: 26,                    // Full theory dimensionality
        signature: { space: 24, time: 2 },   // (24,2) signature
        effective: 13,            // Gauge-fixed shadow dimensionality
        effectiveSignature: { space: 12, time: 1 },  // (12,1) effective
        observable: 4,           // Macroscopic spacetime

        // Brane hierarchy
        nBranes: 4,                // Number of D-branes
        spatialDimsPerBrane: 3, // Spatial dimensions per brane
        timeDims: 1,              // Shared time dimension

        // Derived
        get dEff() { return 12; },           // Effective spatial dimensions for MEP
        get totalCheck() { return this.nBranes * this.spatialDimsPerBrane + this.timeDims; } // Should = 13
    },

    // ================================================================
    // SPINOR STRUCTURE
    // ================================================================

    spinors: {
        full26D: 8192,      // 8192 components in Cl(24,2)
        effective13D: 64, // 64 components after Sp(2,R) gauge fixing

        // Clifford algebras
        clifford26D: "Cl(24,2)",
        clifford13D: "Cl(12,1)",
        clifford4D: "Cl(3,1)",

        get reductionRatio() { return this.full26D / this.effective13D; }  // 128
    },

    // ================================================================
    // TOPOLOGY & GENERATIONS
    // ================================================================

    topology: {
        // Calabi-Yau Hodge numbers
        h11: 4,
        h21: 0,
        h31: 72,

        // Euler characteristic
        chiEffective: 144,  // 144 (effective)

        // Flux quantization
        fluxReduction: 2,

        // Generations
        generations: 3,  // 3 (exact match with SM)

        // SO(10) representations
        adjointDim: 45,      // 45
        spinorDim: 16,        // 16
    },

    // ================================================================
    // FUNDAMENTAL SCALES
    // ================================================================

    fundamentalScales: {
        mPlanck: 1.2195e+19,          // GeV (PDG 2024)
        mStar: 1e+19,              // GeV (13D fundamental scale)
        mGUT: 1.8e+16,               // GeV (grand unification)
        alphaGUT: 0.0411522633744856,       // GUT fine structure constant
        alphaEM: 0.0072973525692838015,          // QED fine structure constant
    },

    // ================================================================
    // DARK ENERGY (DESI 2024 Compatible)
    // ================================================================

    darkEnergy: {
        // Theoretical values (derived from MEP + two-time)
        w0Numerator: -11,
        w0Denominator: 13,
        w0: -0.8461538461538461,             // -11/13 ≈ -0.846
        wa: -0.75,           // -0.75 (exact DESI match!)

        // Observational values (DESI 2024)
        w0Observed: -0.827,
        w0Error: 0.063,
        waObserved: -0.75,
        waError: 0.3,

        // Cosmological parameters
        omegaLambda: 0.6889,   // Dark energy density
        omegaMatter: 0.3111,   // Matter density
        omegaBaryon: 0.0486,   // Baryon density
        H0: 67.4,                      // km/s/Mpc
    },

    // ================================================================
    // THERMAL TIME HYPOTHESIS
    // ================================================================

    thermalTime: {
        alphaT: 2.7,     // 2.7 (Z₂-corrected canonical)
        alphaTBase: 2.5,      // 2.5 (without Z₂)
        z2Correction: 0.2,   // 0.2

        // Epoch-dependent values
        alphaTz0: 1.67,          // 1.67 (Λ-dominated, z=0)
        alphaTz1: 2.38,          // 2.38 (transition, z=1)
        alphaTz2: 2.59,          // 2.59 (matter-dominated, z=2)
        alphaTHighZ: 2.7,   // 2.7 (deep matter, z>3)

        get waFormula() { return `w_a = w_0 × α_T / 3 ≈ -0.846 × 2.7 / 3 ≈ -0.76`; }
    },

    // ================================================================
    // F(R,T,τ) MODIFIED GRAVITY
    // ================================================================

    modifiedGravity: {
        alphaRSquared: 0.0045,     // [M_Pl^-2] 1-loop quantum correction
        betaMatter: 0.15,            // Dimensionless, breathing mode
        gammaMixed: 0.0001,            // [M_Pl^-2] RT coupling
        deltaOrthoTime: 1e-19,   // [seconds] ∂_τ coefficient

        phi0Breathing: 0.075, // Breathing mode VEV
        nEffPneuma: 64,       // Effective DOF in loops
    },

    // ================================================================
    // MULTI-TIME STRUCTURE
    // ================================================================

    multiTime: {
        // Coupling constants
        g: 0.1,                 // 0.1 (multi-time coupling)
        eFermi: 1.0,               // 1.0 TeV (Fermi energy)

        // Gravitational wave dispersion
        xi: 10000000000.0,              // 10^10 (quadratic coefficient)
        eta: 0.1,             // 0.1 (linear: g/E_F)
        etaBoosted: 1000000000.0,       // 10^9 (asymptotic safety)

        // Orthogonal time parameters
        deltaTOrtho: 1e-18,    // 10^-18 seconds
        rOrtho: 1.0,               // Normalized radius

        // LISA frequencies
        kLISAMin: 0.0001,          // 10^-4 Hz
        kLISATypical: 0.001,  // 10^-3 Hz
        kLISAMax: 0.1,          // 10^-1 Hz

        // Mirror sector
        thetaMirror: 0.0,  // 0.0 radians (no mixing)
        theta45Deg: 0.7853981633974483,    // π/4 for examples
    },

    // ================================================================
    // MODULI STABILIZATION (KKLT)
    // ================================================================

    moduli: {
        // SUSY breaking
        fTerm: 1.0,       // Normalized F-term
        fTermPhysical: 10000000000.0, // 10^10 GeV^2

        // Swampland parameters
        aSwampland: 1.4142135623730951,      // √(26/13) = √2 ≈ 1.414
        swamplandBound: 0.816496580927726, // √(2/3) ≈ 0.816

        // Potential terms
        kappaUplift: 1.0,      // 1.0
        bInstanton: 1.0,        // 1.0
        muPeriodic: 0.5,        // 0.5

        // Condensate
        lambdaCoupling: 0.5, // 0.5 TeV^-2
        vVEV: 2.0,                     // 2.0 TeV
        tOrthoNorm: 1.0,  // 1.0

        condensateGap: 0.9090909090909091, // ~0.909 TeV
    },

    // ================================================================
    // LANDSCAPE & MULTIVERSE
    // ================================================================

    landscape: {
        // Vacuum counting
        nVacExponent: 500,      // 500 (N_vac ~ 10^500)
        landscapeEntropy: 1151.2925464970228, // ~1151.29

        // Coleman-De Luccia tunneling
        sigmaTension: 1.0,       // Normalized
        deltaVMultiverse: 10000000000.0, // Normalized

        // CMB bubble collisions
        bubbleRadiusMpc: 100,   // 100 Mpc
        cmbTemperatureUK: 100, // 100 μK

        get euclideanAction() { return 1.3323965941470632e-28; },
        get tunnelingRate() { return 1.0; },
    },

    // ================================================================
    // v6.1 NEW PREDICTIONS (Testable!)
    // ================================================================

    v61Predictions: {
        // Kaluza-Klein modes (LHC-testable)
        mKKCentral: 5.0,       // 5.0 TeV
        mKKMin: 3.0,               // 3.0 TeV (95% CL)
        mKKMax: 7.0,               // 7.0 TeV (95% CL)
        mKKCurrentBound: 3.5, // 3.5 TeV (ATLAS/CMS)

        // GW dispersion (LISA-testable)
        etaBaseline: 0.1,      // 0.1
        etaBoosted: 1000000000.0,        // 10^9
        etaEffectMin: 1e-29,   // 10^-29
        etaEffectMax: 1e-20,   // 10^-20

        // SME Lorentz violation (Collider-testable)
        cMuNuFermion: 1e-05,  // 10^-5
        cEMuRatio: 2e-05,        // 2×10^-5
        sMuNuGW: 1e-15,            // 10^-15

        // Quantum nonlocality (Lab-testable)
        chshDeltaOrtho: 1e-05, // 10^-5
        chshPredicted: 2.828028,    // 2.828028
        chshStatisticsRequired: 100000000000.0, // 10^11

        // Mirror sector (CMB-testable)
        deltaNEffMin: 0.08,    // 0.08
        deltaNEffMax: 0.16,    // 0.16
        deltaNEffCentral: 0.12, // 0.12
    },

    // ================================================================
    // NEUTRINO SECTOR
    // ================================================================

    neutrinos: {
        // Normal Hierarchy prediction (PRIMARY FALSIFICATION TEST)
        hierarchy: "Normal",  // "Normal"

        // Mass spectrum
        mNu1: 0.001,        // 0.001 eV
        mNu2: 0.009,        // 0.009 eV
        mNu3: 0.05,        // 0.050 eV
        sumMNu: 0.06,    // 0.060 eV (WARNING: not unique)

        // Oscillation data
        deltaMSq21: 7.5e-05, // 7.5×10^-5 eV^2
        deltaMSq31: 0.0025, // 2.5×10^-3 eV^2

        // Seesaw scale
        mRHNeutrino: 100000000000000.0,     // 10^14 GeV
    },

    // ================================================================
    // CMB BUBBLE COLLISIONS
    // ================================================================

    cmbBubbles: {
        // Gaussian random field
        sigmaCMBRMS: 0.003,         // 3×10^-3
        thetaSpotDeg: 1.0,       // 1.0 degrees
        thetaSpotRad: 0.017,       // 0.017 radians
        nMinimaDensity: 1650,   // 1650 sr^-1
        skyAreaSR: 12.566370614359172,             // 4π ≈ 12.566

        // Anomaly thresholds
        delta3Sigma: 0.009,          // 9×10^-3
        delta5Sigma: 0.015,          // 15×10^-3

        // Bubble signatures
        deltaDiskCollision: 0.1, // 0.1
        fNLBubble: 100,             // 100

        // Poisson statistics
        lambdaThreshold: 0.001, // 10^-3

        kurtosisExcess: (nDisk, delta, sigma) => {
            return 3 + nDisk * Math.pow(delta/sigma, 4);
        }
    },

    // ================================================================
    // PROTON DECAY
    // ================================================================

    protonDecay: {
        tauProton: 3.5e+34,           // 3.5×10^34 years (central)
        tauLowerBound: 1.67e+34, // 1.67×10^34 years (Super-K)

        branchingRatio: (theta) => {
            const beta = Math.cos(theta);
            return 1 / (1 + beta * beta);
        }
    },

    // ================================================================
    // COMPUTATIONAL SETTINGS
    // ================================================================

    computational: {
        // QuTiP simulations
        nQutipToy: 4,         // 4
        nQutipProduction: 10, // 10

        // Time evolution
        timeStart: 0,              // 0
        timeEnd: 10,                  // 10
        timeSteps: 100,              // 100

        // Tolerances
        toleranceUnitarity: 1e-10,     // 10^-10
        toleranceConvergence: 1e-08, // 10^-8

        // Limits
        aLimitExponent: 10,   // 10 (exp(10) for a→∞)

        // SymPy
        sympyPrecision: 10, // 10
    },

    // ================================================================
    // METADATA
    // ================================================================

    metadata: {
        version: "6.1",
        lastGenerated: "2025-11-27 19:22:34",
        generatedFrom: "config.py via generate_js_constants.py",
        frameworkName: "Principia Metaphysica",

        // Validation checks
        validate: function() {
            const checks = {
                swampland: this.parent.moduli.aSwampland > this.parent.moduli.swamplandBound,
                generations: this.parent.topology.generations === 3,
                dimensions: this.parent.dimensions.totalCheck === 13
            };

            const allPass = Object.values(checks).every(v => v === true);
            return { checks, allPass };
        }
    }
};

// Add parent reference for metadata validation
TheoryConstants.metadata.parent = TheoryConstants;

// Export for use in other JS files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = TheoryConstants;
}

// Display configuration summary
console.log('SUCCESS: Principia Metaphysica Theory Constants loaded');
console.log(`  Framework: v${TheoryConstants.metadata.version}`);
console.log(`  Generated: ${TheoryConstants.metadata.lastGenerated}`);
console.log(`  Dimensions: ${TheoryConstants.dimensions.full}D -> ${TheoryConstants.dimensions.effective}D -> ${TheoryConstants.dimensions.observable}D`);
console.log(`  Generations: ${TheoryConstants.topology.generations}`);
console.log(`  Swampland: a = ${TheoryConstants.moduli.aSwampland.toFixed(3)} > ${TheoryConstants.moduli.swamplandBound.toFixed(3)} PASS`);
